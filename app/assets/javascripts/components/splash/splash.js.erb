angular
  .module('eventPlanner')
  .component('splash', {
    templateUrl: '<%= asset_path("components/splash/splash") %>',
    controller: SplashController
  });

  SplashController.$inject = ['$http'];

  function SplashController($http) {

  }

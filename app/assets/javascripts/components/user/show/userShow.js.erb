angular
  .module('eventPlanner')
  .component('userShow', {
    templateUrl: '<%= asset_path("components/user/show/userShow") %>',
    controller: UserShowController
  });

UserShowController.$inject = ['$http', '$stateParams'];

function UserShowController($http, $stateParams) {
  var vm = this;

  vm.user = {};
  vm.message = "Testing User";

	activate()

	function activate() {
		loadUser()
	}

	function loadUser() {
    $http.get('/api/users/' + $stateParams.id).then(function(resp) {
      console.log(resp.data);
      vm.user = resp.data;
    })
	}
}
